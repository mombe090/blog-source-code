apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pg-ui
  namespace: cloudnative-pg
spec:
  parentRefs:
    - name: contour-gateway-api
      sectionName: tcp-routes
      namespace: contour-system
  hostnames: ["cloudnative-pg.tuto.mombesoft.com"]
  rules:
    - backendRefs:
        - name: cloudnative-pg-cluster-rw
          port: 5432
